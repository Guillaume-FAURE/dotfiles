#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
#define PLUS(x, y) ((x) + (y))
#define FACTORIAL(n) (
    for (int i; i < n;i++) {
        
    }
)

int main(void) {
    int fact11 = FACTORIAL(3);
    printf("%i\n", fact11);
    return 0;
}