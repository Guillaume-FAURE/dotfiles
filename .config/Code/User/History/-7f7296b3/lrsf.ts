function longestPalindrome(s: string): string {
    const stringLength = s.length;
    const arrayString = s.split('');
    const currentPalindrome: Array<string></string> = []
    const maxPalindrome = []
    for (let i = 0; i < stringLength; i += 1){
        
    }
}
